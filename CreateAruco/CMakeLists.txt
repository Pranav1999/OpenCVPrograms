cmake_minimum_required(VERSION 2.8)
project( CreateAruco )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( CreateAruco CreateAruco.cpp )
target_link_libraries( CreateAruco ${OpenCV_LIBS} )
